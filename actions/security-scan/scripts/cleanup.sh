#!/bin/bash
# =============================================================================
# Cleanup
# =============================================================================
# Removes temporary files created during the security scan.
# =============================================================================

rm -f /tmp/security-scan-python.json
rm -f /tmp/security-scan-npm.json
rm -f /tmp/security-scan-results.json
rm -f /tmp/security-scan-requirements.txt
