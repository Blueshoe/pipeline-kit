[project]
name = "security-scan-test-uv"
version = "0.1.0"
description = "Test fixture with intentionally vulnerable dependencies"
requires-python = ">=3.10"
dependencies = [
    "Jinja2==2.10",
    "pyyaml==5.3.1",
    "certifi==2020.12.5",
]

[tool.uv]

[dependency-groups]
dev = []
