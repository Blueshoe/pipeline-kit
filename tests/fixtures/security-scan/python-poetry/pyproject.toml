[tool.poetry]
name = "security-scan-test-poetry"
version = "0.1.0"
description = "Test fixture with intentionally vulnerable dependencies"
authors = ["Test <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
Jinja2 = "2.10"
pyyaml = "5.3.1"
certifi = "2020.12.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
